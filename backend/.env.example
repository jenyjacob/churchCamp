# Copy to .env and fill in your values — never commit the real .env to git

DATABASE_URL=mysql+pymysql://username:password@localhost:3307/churchcamp
JWT_SECRET_KEY=replace-with-long-random-string-min-32-chars
SECRET_KEY=replace-with-another-long-random-secret
CORS_ORIGINS=http://localhost:3000
